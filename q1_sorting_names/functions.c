#include <stdio.h>
#include <string.h>

/* callback function for sorting based on order */
int sort_names(int (*sort)(char *[]), char *names[])
{
sort(char *names[]);
return (0);
}

/* counts num of names in array */
int count_names(char *names[])
{
int count = 0;
while (*names)
{
i++;
names++;
}
return (count);
}


